# Options API render

## Vue SFC JSX

<<< ../../demo/src/components/options/OptionsRenderJsx.vue

## Vue SFC TSX

<<< ../../demo/src/components/options/OptionsRenderTsx.vue

没有显式 `render(h)` 参数时，插件会根据环境自动注入 `h`；TSX 类型保留给 Vite Oxc 的后续阶段。
